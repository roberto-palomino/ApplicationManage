{
  "name": "gestion-ingresos",
  "version": "1.0.0",
  "description": "Application to manage and administer income and savings",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --app-index index.html --watch --open --port 8081",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "management",
    "savings-income"
  ],
  "author": "Roberto Palomino",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.1.35"
  }
}
